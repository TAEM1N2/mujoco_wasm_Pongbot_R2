Place the implicit walking policy files here:

- encoder.onnx
- policy.onnx

The wasm demo loads these paths directly from the browser:

- ./assets/policies/implicit/encoder.onnx
- ./assets/policies/implicit/policy.onnx
